# Rejected automated design system

`python3 ~/.grok/skills/ui-ux-pro-max/scripts/search.py "industrial energy equipment B2B manufacturer tap changer" --design-system -p "Huaming"`

Returned Organic Biophilic, mint `#059669`, cream-green background, Syncopate / Space Mono.

That is a wellness / eco palette. It is not a tap-changer maker. Do not implement it.

The vote in `docs/plans/` + `docs/votes/` owns color, type, and density.
