# Audit — family list pages

Homepage 02 and product detail pages not rewritten. 3:2 contain not undone. InnerTeaser not rebuilt.

## What shipped

- `/products/oltc/`, `/octc/`, `/accessories/`, and family URLs that use ProductCategory share the same catalogue paint as `/products/`.
- Tables kept: Type / Kind / Note / PDF. Navy head, ink body, steel links. PDF goes to `/downloads/`.
- “Technical data at a glance” grey MR band is a short heading: “Types in this family.”
- Tabs stay On-load / De-energized / Accessories. No new families.
- Type stills under the tables still 3:2 contain (ProductBox + existing CSS).

## Fine

- `astro build`: 61 pages.
- `/` still sketch 02.
- CM2/CV2 detail pages untouched.

## Leftover

- OLTC still slots the laboratory TextMedia block (engineers photo). Not this slice.
- InnerTeaser tiles at the bottom of family pages (already navy).
- Family stage still sits above the title (contain, not cover).

## Worse?

- No. Tables still list every type. A buyer can still open a type page or Downloads.
