# Morning sweep — overnight-unclone

Walked as a foreign buyer: `/` → `/products/` → `/products/oltc/` → `/products/cm2/` → `/downloads/` → `/contact/` → `/about/` → `/news/`.

Grep of `src/` (not `mirror/`): no VACUTAP, OILTAP, ECOTAP, TESSA, THE POWER BEHIND POWER, `#e5bb29`, `#FD7702`, Playfair, or Plex Serif in copy or paint. The word “Reinhausen” still appears only as leftover CSS class names (`sitereinhausen_*`) in `layout.min.css`. A buyer does not see those.

No cheap leak to patch. Site not torn up.

## 1. What a buyer can do this morning

- Open the English catalogue on a navy bar. First screen is a CM2 still, whole unit, 3:2 contain.
- Read lead time (4–6 weeks), PDFs, Shanghai factory, Singapore mail.
- Open Types. Filter On-load / De-energized / Accessories. Open a family table (type / kind / PDF).
- Open CM2. Facts table, Downloads tab, related types, 3:2 still.
- Download PDFs with no email gate. Write Singapore (`intl@huaming.com`).
- Read About (factory + HQ) and News (12 live-newsroom articles).

## 2. Fine (do not touch)

- Homepage 02.
- `/products/` grid and family tables.
- PDP facts, PDFs, stills.
- Downloads, Contact, About, News, search, imprint, privacy.

## 3. Small leftover

- InnerTeaser tiles at the foot of many inner pages (already navy, not gold).
- Bare `/404` can hit Astro’s default; `/404/` is the catalogue page.
- Some SVG files are still named `icon_arrow-link-yellow.svg`. The paint is steel `#0071A9`.
- PDP “Product Information” still uses pill tabs (Technical Data / Downloads).
- Bootstrap `#ffc107` remains in minified layout CSS (framework warning, not a brand arrow).
- `sitereinhausen_*` class names in old CSS. Hidden from the buyer.

## 4. Do not merge to main

Eric decides. This branch is `overnight-unclone` only.

## 5. Tip SHA

`59e6a20` — Paint search, 404, imprint, and privacy to the catalogue.
