# Audit — `/products/` listing chrome

Homepage 02 not restyled. Product detail pages (CV2, CM2, …) not rewritten. 3:2 contain not undone.

## What shipped

- `/products/` is the same export catalogue as `/`: IBM Plex Sans, navy bar, ink on white.
- Wordcloud, MR introtext, and the old subnav are gone.
- Quiet tabs: On-load / De-energized / Accessories. Default shows on-load types. Tabs also go to `/products/oltc/`, `/octc/`, `/accessories/`.
- Type grid: 3:2 contain stills, Huaming names, one-line fact. PDF and Singapore links at the bottom.
- Family list pages (`/products/oltc/` and the rest that use ProductCategory) get the same title + tab paint. Tables and type cards stay.

## Fine

- `astro build`: 61 pages.
- `/` still navy bar + CM2 still + type strip + numbered news.
- CM2 detail still 3:2 contain.

## Leftover

- Family pages still have the old tables, “Technical data at a glance” band, and InnerTeaser tiles. Out of this slice.
- ProductBox on family pages still uses MR card markup; photos remain contain.

## Worse?

- No. Types are still listed. Taxonomy unchanged. A buyer can reach PDFs and each type page.
