# Cycle 8 — UX

Vs LOOP lock: chrome → stage (photo or dark band, type bottom-left, one CTA) → photo teasers (full-bleed + shade or empty dark band) → list last. Veto polaroid. Veto white + H1 + table.

Read: remaining 390s + WSL desk. WSL CTA is Leaflet (`WSL-Leaflet.pdf`). Contact 390: form is the action, no stage button. WDG / ZWC and SHM-D / SHM-X empty dark on purpose.

## Must-fix

None. Every 390 has a stage. Teasers are full-bleed + shade or empty dark, not polaroid. No white + H1 + table. List/spec after teasers.

## Should-fix

1. More 390 All types: six rows, stops at WDG. Desk still lists CZ, SHGV, HMDK, HWDK, ZWC, SHM-D / SHM-X / SHM-KX, ET-SZ6, HMC-3C, ZXJY. Teaser 3/4 captions some of those names; the list does not.

2. Careers 390: Singapore and Write are empty dark. Legal (no still). Two of three teasers have no photo.

## Pass / fail vs MR

| Page | Verdict | Why |
|------|---------|-----|
| Home 390 | **Pass** | Photo stage, type BL, dek, one CTA (`Find a type`). Four full-bleed type teasers. |
| Products 390 | **Pass** | Photo stage, type BL, one CTA. 4 family photoTeasers, All types last. |
| Downloads 390 | **Pass** | Factory stage, type BL, CTA `Find a type`. 3 kind teasers, file list last. WSL row is Leaflet. |
| Contact 390 | **Pass** | Factory stage, type BL, no stage button. 3 job photo teasers, form is the action. |
| About 390 | **Pass** | Factory stage, type BL, one CTA. Numbers, Shanghai photo, Singapore empty dark. |
| Careers 390 | **Pass** | Factory stage, `Work on the switch.`, one CTA. Shanghai photo; SG + Write empty dark. |
| WSL 390 | **Pass** | Studio split (white JPEG), type, CTA Leaflet. WDG / ZWC empty dark, CM photo, spec last. |
| WSL desk | **Pass** | Chrome, dark + white JPEG, type BL, CTA Leaflet. WDG / ZWC empty dark, CM photo, spec last. |
| CMA7 390 | **Pass** | Cabinet stage, type BL, CTA Operating instructions. HWV photo; SHM-D / SHM-X empty dark. Spec last. |
| CM2 390 | **Pass** | Dark product stage, type BL, one CTA, 3 sibling photos, spec last. |
| More 390 | **Pass** | Factory stage, type BL, one CTA, 2×2 photo teasers (dry tile = factory), compact rows last. List is a stub. |
| Learn 390 | **Pass** | Substation stage, type BL, one CTA. 3 photo teasers. 02: `CV2, CM2, SHZV, HWV. Then CM, WSL, CMA7.` |
| Not found 390 | **Pass** | Photo stage, type BL, one CTA, three real types as photoTeaser. |
