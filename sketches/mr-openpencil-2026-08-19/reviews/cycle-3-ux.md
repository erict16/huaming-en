# Cycle 3 — UX

Vs LOOP lock (Reinhausen architecture): chrome → **stage** (photo or dark band, type bottom-left, one CTA) → **photo teasers** → list last. Veto: white + H1 + table.

Read: all `preview-*.png` in this folder (desk + `preview-mobile.png`). Slice 4–5 is WSL / CMA7 / CM / More + inner-page mobile.

## Must-fix

1. **News — no photographic stage.** Ink H1 slab, type high in the band, CTA “About”. Cards are fine; the stage is a title bar. Rebuild: full-bleed Longdong or factory, type bottom-left, one CTA, then the three dated teasers.

2. **`preview-mobile.png` (home) is still the handbook.** Stage then four white type cards. No family photo teasers. HWV still reads “Horizontal. Drive included.” Products / Downloads / About / Contact / Projects / Learn **390** already do stage → photos. Rebuild home 390 to that stack (or re-export if the `.fig` is already fixed and this PNG is stale).

3. **WSL / CMA7 / CM stages are white stills + gray mid-band.** CV2 / HWV sit type on a **dark** product stage. These three leak the old white-PDP: pale plate, H1 on a veil, then a spec table. Same `darkPhoto` as CV2 so the stage is not empty-white.

4. **CMA7 primary CTA is “Technical data PDF”.** MDU page. The only file is operating instructions. Button: `Operating instructions` (or Downloads). Do not put a TD CTA on a drive cabinet.

5. **About locations are still white address cards.** Cycle-1 leftover. After the factory stage, Shanghai / Singapore must be **photo teasers**, then the address line. Not two outlined text boxes.

## Should-fix

- Search is still a label on every desk chrome.
- Home desk footer is two companies; inner pages are a thin mail strip. Same ink footer.
- CMA7 / WSL sibling plates mix white cabinets with dark stills (cycle-2 HWV leftover).
- More types: long name list is allowed **after** the 2×2; the OCTC tile should not caption SHM-D on a cage photo (see industrial).

## Pass / fail vs MR

| Page | Verdict | Why |
|------|---------|-----|
| Home | **Pass** | Stage photo, impulses, 2×2 families, type row, numbers, 3-col close. The lock. |
| Products | **Pass** | Stage → 2×2 photos → type list. Mobile column matches. |
| Downloads | **Pass** | Cycle-1 veto rebuilt: factory stage, 3 kind teasers, table last. |
| About | **Pass** (must-fix 5) | Factory stage + numbers. Not a table page. Location tiles still white. |
| Contact | **Pass** | Factory stage, type bottom-left, 3 job photos, form after. |
| Projects | **Pass** | Longdong stage, 3 teasers, numbers. |
| News | **Fail** | Dark H1, no photo stage. |
| Careers | **Pass** | Factory stage + 3 photo tiles. |
| Learn | **Pass** | Substation stage + 3 step teasers. |
| CV2 / CM2 / SHZV / HWV | **Pass** | Dark product stage, type bottom-left, 3 teasers, spec last. |
| WSL / CMA7 / CM | **Fail** | Order is right; stage is white still + gray H1 (must-fix 3). |
| More types | **Pass** | Factory stage + 2×2, then compact rows. |
| Home mobile (`preview-mobile.png`) | **Fail** | White type list. No teasers. |

No page is the full white+H1+table veto except the **home 390** list and the **pale PDPs**. Do not ship News, home mobile, or WSL/CMA7/CM stages until 1–3 are in the `.fig`.
